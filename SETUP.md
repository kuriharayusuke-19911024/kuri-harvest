# 丹波栗 収穫管理アプリ — 導入手順

株式会社丹波農商 / 3人でスマホ・PCから共有して使うための設定手順です。
上から順にやれば動きます。**先に GAS（②）→ 次に GitHub（③）** の順で進めてください。

---

## ① ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体（単一ファイル）。GitHub Pages で公開する |
| `Code.gs` | Google Apps Script のコード。スプレッドシート側に貼る |
| `kuri-harvest.html` | 元のプロトタイプ（localStorage版）。保存用・使いません |
| `SETUP.md` | この手順書 |

---

## ② Google 側の作業（先にこちら）

### 2-1. スプレッドシートを作る

1. Google ドライブで新しいスプレッドシートを作成
2. ファイル名を **`栗収穫管理`** にする

シート（収穫・出荷・買取・設定）は **手で作らなくて大丈夫です**。
アプリが最初にアクセスしたときに自動で作られます。

### 2-2. Apps Script にコードを貼る

1. そのスプレッドシートを開いたまま、メニューの **拡張機能 > Apps Script**
2. 出てきたエディタの `function myFunction() {}` を**全部消す**
3. `Code.gs` の中身を**全部コピーして貼り付け**
4. 保存（フロッピーのアイコン、または Ctrl+S）
5. プロジェクト名を聞かれたら `栗収穫管理API` などでOK

### 2-3. 先に一度だけ動かして権限を許可する

1. エディタ上部の関数プルダウンで **`setup`** を選ぶ
2. **実行** を押す
3. 「承認が必要です」→ **権限を確認** → 自分のGoogleアカウントを選ぶ
4. 「このアプリは確認されていません」と出たら
   → 左下の **詳細** → **「栗収穫管理API（安全ではないページ）に移動」** → **許可**
5. 実行ログに `OK: 栗収穫管理` と出て、スプレッドシートに
   **収穫 / 出荷 / 買取 / 設定** の4シートができていれば成功

> ここで許可しておくと、次のデプロイがスムーズです。

### 2-4. ウェブアプリとしてデプロイする

1. エディタ右上の **デプロイ > 新しいデプロイ**
2. 歯車マーク（種類の選択）→ **ウェブアプリ**
3. 設定：
   - 説明：`v1`（何でもOK）
   - **次のユーザーとして実行：自分**
   - **アクセスできるユーザー：全員**
4. **デプロイ** を押す
5. 表示された **ウェブアプリURL** をコピー
   （`https://script.google.com/macros/s/AKfycb.../exec` の形）

### 2-5. URL を index.html に貼る

`index.html` をテキストエディタ（メモ帳でも可）で開き、**`API_URL` で検索**します。
`<script>` が始まってすぐ、**236行目あたり**にこの行があります。

```js
const API_URL='PASTE_YOUR_GAS_WEB_APP_URL_HERE';
```

`PASTE_YOUR_GAS_WEB_APP_URL_HERE` を、2-4でコピーしたURLに置き換えます。

```js
const API_URL='https://script.google.com/macros/s/AKfycb.../exec';
```

> **シングルクォート `'` は消さないでください。**

この時点で `index.html` をダブルクリックしてブラウザで開けば、もう動きます。
（画面下に「読み込みました」ではなく「読み込めませんでした」と出る場合は URL を確認）

---

## ③ GitHub に置いて3人で使えるようにする

### 3-1. リポジトリを作る

1. GitHub にログイン → 右上 **+** → **New repository**
2. Repository name： **`kuri-harvest`**
3. **Public** を選ぶ（Pages を無料で使うため）
4. **Create repository**

### 3-2. アップロードする

**かんたんな方法（ブラウザだけ）**

1. できたリポジトリの画面で **Add file > Upload files**
2. `index.html` をドラッグ＆ドロップ（`Code.gs` と `SETUP.md` も一緒に入れておくと安心）
3. 下の **Commit changes**

**コマンドでやる場合**

```bash
cd "C:/Users/user/Documents/kuri-harvest"
git init
git add index.html Code.gs SETUP.md
git commit -m "丹波栗 収穫管理アプリ（GAS連携版）"
git branch -M main
git remote add origin https://github.com/kuriharayusuke-19911024/kuri-harvest.git
git push -u origin main
```

### 3-3. GitHub Pages を有効にする

1. リポジトリの **Settings** タブ
2. 左メニューの **Pages**
3. Source： **Deploy from a branch**
4. Branch： **main** / フォルダ： **/ (root)** → **Save**
5. 1〜2分待つと、同じ画面の上に公開URLが出ます

```
https://kuriharayusuke-19911024.github.io/kuri-harvest/
```

### 3-4. 3人に共有する

このURLを3人に送って、**スマホのホーム画面に追加**してもらうとアプリのように使えます。

- iPhone（Safari）：共有ボタン → 「ホーム画面に追加」
- Android（Chrome）：右上「︙」→ 「ホーム画面に追加」

---

## ④ 動作確認チェックリスト

3人のうち2台で同時に開いて確認してください。

- [ ] Aさんが「収穫」を1件記録する
- [ ] Bさんが画面を**再読み込み**すると、その記録が見える
- [ ] スプレッドシートの「収穫」シートにも同じ行が増えている
- [ ] 「買取」を記録すると納品書が出て、印刷/PDF保存できる
- [ ] 「設定」で園地を1つ追加 → 別の端末を再読み込みすると増えている

---

## ⑤ 使ううえでの注意

- **画面は自動更新されません。** 他の人の入力を見るには**再読み込み**してください。
- 保存中はボタンが押せなくなり、「保存中…」と出ます。数秒かかることがあります。
- 「保存できませんでした」と出たときは、**入力内容はそのまま残っています**。
  電波を確認してもう一度ボタンを押してください。
- 「設定 > 全データ削除」は**記録（収穫・出荷・買取）だけ**を消します。
  設定（園地・品種・サイズ・買取先・単価）とシート自体は残ります。確認は2回出ます。
- 記録が多いと「全データ削除」は1件ずつ消すので時間がかかります（進捗が出ます）。
- **バックアップ**は「設定 > バックアップ（JSON）」か、
  スプレッドシートの **ファイル > コピーを作成** が確実です。

### ⚠ セキュリティについて（要確認）

デプロイ設定を「アクセスできるユーザー：**全員**」にしているため、
**GASのURLを知っている人は誰でも**このデータを読み書きできます。
そのURLは公開された `index.html` の中に書かれているので、
リポジトリを Public にすると理屈のうえでは第三者が到達できます。

買取先の名前・単価・金額が入るデータなので、気になる場合は次のどちらかを検討してください。

1. **合言葉（トークン）を付ける** — `index.html` と `Code.gs` の両方に同じ合言葉を持たせ、
   一致しないリクエストを GAS 側で拒否する（対応可能です。言ってください）
2. **リポジトリを Private にする** — ただし GitHub Pages の Private 公開は有料プランが必要

---

## ⑥ コードを直したくなったら

- **画面・文言を変える** → `index.html` を編集して GitHub に再アップロード
- **保存の仕組みを変える** → `Code.gs` を編集したあと、
  **デプロイ > デプロイを管理 > 鉛筆マーク > バージョン「新バージョン」→ デプロイ**
  （新しいデプロイを作るとURLが変わってしまうので注意）
